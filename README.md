Билет №4.5
Задание 1: Уплотнить строку S1, удалив символы, встречающиеся в строке S2.
Идея реализации: Инициализировать s1,s2 которые отвечают за строчку в которой менять и строчку символов которые надо будет менять. Создать массив s3 в котором на места кодов по таблице ascii будем ставить 1 (это означает что символ есть в строчке s2) и проходим по строке s1. Если индекса элемента из строки s1 нет в нашем массиве то перезаписывааем с самого начала строчку и в конце добавляем конец '\0' и выводим строчку ( реализация в функции и функция не говорит, а возвращает полученную строчку s1)

Тесты:
// Abcdef bcd / Aef - Дано одно слово и все буквы англ
// My name is Kirill ail / My nme s Kr - Дано несколько слов пробелы между ними остались все буквы англ
// My name is Kirill ail  /MynmesKr - Дано несколько слов пробелы между ними исчезли тк в s2 есть пробел все буквы англ
// Abcd Abcd / - пустота тк все все буквы совпадают в строчке s1 и в строчке s2 , все буквы англ
// 1234Abcd6 1234 / Abcd6 - Удалились числа , все буквы англ
// АБВГД / АБВГД - Все буквы русские и строчки s1, s2 равны -> Тк нет английских букв обработка прошла успешно и ответ вывелся верный , пустая строчка, но это лишь случайность
// Привет xyz / Привет байты не пересекаются а значит слово полностью остается - в s1 хранятся русские буквы , в s2 англ
//  Hi Мир Hi / Мир удаляются только байты Hi(те англ) русские не трогали
// Test Тест т / Test Те� у нас происходит поломка, тк т удаляет и байты другой буквы из-за чего появляется проблема
// NULL abs / Error with NULL! передаем NULL и возникает ошибка
// пустота abcd / передаем пустоту и тк нечего удалять получается пустота / обратно наборот ничего не удалит и выведет все символы
// a@b#c$ @#$ / получаем ответ abc тк спецсимволы хорошо удалились
// aaa a / пустота , тк повторы все удалились отработала программа корректно
// Аа а / выдается � тк опять происходит поломка
